<div class="container">
    <h1>Create New Event</h1>

        <%= render "form.html",
        Map.put(assigns, :action,
        Routes.event_path(@conn, :create)) 
        %>

    <p><%= link "Back", to: Routes.page_path(@conn, :index) %></p>
</div>